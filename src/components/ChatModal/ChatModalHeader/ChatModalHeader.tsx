import React from "react";
import s from "./ChatModalHeader.module.scss";

export const ChatModalHeader = () => {
  return (
    <div className={s.chat_modal_header}>
      <button className={s.chat_modal_header__icon} />
    </div>
  )
}